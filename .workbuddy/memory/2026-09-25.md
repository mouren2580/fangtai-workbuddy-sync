# 2026-09-25 工作日志

## 看板数据更新至 2026-09-24（BUILD 20260925-0849）

- Excel `2026年9月西北服务产品(3).xlsx`（md5 `4d722877...`，12.4MB，Sep 25 08:43 修改）各表最大日期均为 **2026-09-24**，与「更新日−1」铁律吻合。
- 校验：用新 Excel 按旧截止日 **2026-09-17** 重算，与上一版（BUILD 20260918-1235）语义比对：
  - `MDATA_2026_09_W`、`MONTHLY_FOUR.workorder`、`MONTHLY_FOUR.valueadded` **逐项一致**；
  - B / E / valueparts / bigcare / EXTEND_TIME / CLEANING **仅明细行序不同**（跨 Excel 不可复现，多重集一致）；
  - **仅 D/T 有真实差异**（网点 92→95、工程师 216→222、报表总合计 ¥356,584.74→¥498,445.70）——「服务产品收入统计·报表全量」口径，换快照必然变，属预期。
  - 列序自检 `[OK]`（9-18 改的表头名解析生效，本次未再错位）。
- 构建（截止 9-24，周期 8.28–9.27，时间进度第 28 天）：
  - 工单 **20,406**（= Excel 工单表有效日期总行数，**完全自洽**）/ 止回阀 **1,300** / 烟机安装 6,906 / 占比 **18.82%**
  - 大保养 **16÷670 = 2.39%**（总单 682）
  - 延保明细 **181 条 ¥67,629.80** / 清洗保养明细 **669 条**
  - 增值产品 2,065 / 增值配件当月 2,800
  - D 报表全量 **¥498,445.70**（95 网点）/ T **222 人 ¥410,654.70**
- 历史月回归：1–8 月 MONTHLY_FOUR + year、8 月延保 139 条、8 月清洗 788 条、40 个 `MDATA` 块 **全部完整** ✅；百分比 `toFixed(2)` 与环形图字号 10.5 保留。
- 四端同步：GitHub Pages commit `abdf116`、CloudStudio、本地三副本（md5 一致）、备份仓 Gitee+GitHub `d457559`。

## 本次新踩坑
- CloudStudio 部署再次返回「预留域名未绑定…本次发布已停止」——**仍是假失败**，`curl version.json` 实测已是 `20260925-0849`。确认手段：查 `version.json` 的 `v`/`cut`，不要重试部署。
- `git status` 未列出 `deploy_cs/` 的改动（疑似被 .gitignore 覆盖），不影响线上（部署走工具而非 git）。
