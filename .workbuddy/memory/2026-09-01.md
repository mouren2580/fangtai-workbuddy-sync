# 2026-09-01 工作日志

## 9月数据更新（整板切 2026-09）
- 用户提供 `2026年9月西北服务产品.xlsx`（09-01 11:22 更新，1,909,163 字节），要求更新看板。
- 用户明确「8月的数据已经固定了，这是9月的数据」→ **整板切到9月**（不再保留8月可切换；1-7月历史键 MONTH_DATA 仍保留）。
- Excel 扫描（scan_sep.py / scan_sep_result.txt）：7 个表结构同8月（WMS网点买断配件明细、CSM服务项目、CSM配件、服务产品收入统计、工单自购配件明细、工单、服务技师）。**已完成业务最大日期 = 2026-08-31**（WMS/CSM 完成到8-31）；安装工单明细到 9-05、工单表到 2027-12-20 为「预约/计划/保修」未来值，不计入已完成。
- 截止日口径：按铁律（通知更新日 9-1 前一天 = 8-31），与真实完成数据完全吻合 → **锁 2026-08-31**。
- 服务周期自动变 **8.28–9.27（31天）**（svcPeriod 通用规则：上月28→当月27）；截止 8-31 = 周期第 4 天，剩余 **27 天**。
- 改动（update_sep.py，已校验 xlsxData==9月Excel 一致 2545552 字节、无 2026-08-20 残留）：
  - `xlsxData` 块换成 9月 Excel base64
  - 423行 `DASHBOARD_DATA.meta`：月份 2026-08→2026-09、统计截止日 8-20→8-31
  - monthBadge / CURRENT_MONTH / SAVED_MONTH / 年度视图回退 fallback / MONTH_DATA 键 2026-08→2026-09
  - `rebuildFromWorkbook` + 离线加载截止日块：锁 8-31（注释更新为 9-01 前一天）
  - `EMBED_TARGETS_BY_MONTH` 加 2026-09（**沿用8月目标**，月度目标未另提供）
  - **BASE_MONTH 保持 "2026-08" 不动**（去年同比对比仅8月基准可用，业务规则；9月不对比去年）
- 部署：
  - ✅ CloudStudio `https://0717bc4b30824b8d8a407555473b321e.app.workbuddy.link` 部署成功（已为9月版）
  - ❌ GitHub Pages（fangtai-dashboard）443 超时 6 次未推，待网络恢复补推
  - ✅ Gitee 主仓推送成功 `bbd6f1f..be360ac`
  - ❌ GitHub 备份仓（fangtai-workbuddy-sync）443 超时 3 次未推，本地领先 1 提交待补
- 新增脚本：`scan_sep.py`（严格扫各表 datetime 日期范围）、`update_sep.py`（8月→9月全量文本替换）。

## 待办
- 网络恢复后补推：① GitHub Pages `fangtai-dashboard`；② GitHub 备份仓 `fangtai-workbuddy-sync`（领先 1 提交）。
- PAT 替换（撤销 ghp_...Lp1 改受限 PAT）仍按用户「先不动」保持现状。

## 11:5x 用户发现两网页不同步，补推 GitHub Pages 失败（443 又断）
- 用户反馈「你两不同步」：CloudStudio 已是9月版（截止8-31），GitHub Pages（`mouren2580.github.io/fangtai-dashboard/`）仍停在8-21旧版（截止8-20）——根因是 9-01 11:37 那次 Pages 推送 443 失败、未生效。
- 补推重试：克隆 fangtai-dashboard 成功 → commit `f1d8e67`（9月版）→ push 失败。
  - 根因1（凭据）：`insteadOf` 用 `https://<PAT>@github.com/` 只给用户名、密码位空，Windows 凭据缓存这次没命中 → 弹窗要密码被拒。已改 `git remote set-url origin https://x-access-token:<PAT>@github.com/...`（PAT 放密码位）修正，详见 MEMORY.md 推送模板。
  - 根因2（网络）：改格式后 443 又连不上（12:02–12:06 连续8次超时/重置），与 8-21、9-01 抖动同。
- **当前状态**：`fhd_clone/`（fangtai-dashboard 克隆）含未推的 9月 commit `f1d8e67`、远程已设 x-access-token 格式，**保留未删**，等 443 恢复喊「推 GitHub」即可直接 push（无需重新克隆/覆盖）。
- 两个网页看板目前仍不同步（CloudStudio=9月正确，GitHub Pages=8月旧版），待补推。
