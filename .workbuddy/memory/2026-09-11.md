# 2026-09-11 工作日志

## 16:0x 用户 9-11 BUILD 上线，强刷核对发现 CloudStudio 未同步，已修复对齐
- 用户单位完成看板 BUILD `20260911-0927`（GitHub commit `41896a0e08`，blob 本地逐字节一致校验通过），数据至 **9-10**、截止 **9-10**（铁律：更新日 9-11 前一天）、月份 **2026-09**。
- **机制重大变更**：用户改用「在线版」——`fetch('version.json')` + 手动上传 Excel + **9 月数据硬编码进 JS**（非 base64 内嵌 xlsxData）。`version.json` 仅版本元数据 `{"v":"20260911-0927","cut":"2026-09-10","t":"2026-09-11T09:27:31"}`，不含数据。这与此前我维护的 dashboard_offline.html（内嵌 base64）是两套不同机制。
- 强刷查看（用 **curl 抓源码 + Python 提字段**核对；WebFetch 会把 SPA 当 markdown 化、丢 script 不可靠）：
  - **GitHub Pages**：已是 9-11 最新版（BUILD 标记 + 数据到 9-10 + 9 月 meta 截止 9-10）✅
  - **CloudStudio**：抓取仍是 9-07 内嵌旧版（截止 9-06、无 BUILD 标记）❌ 用户以为"已同步"但实际未同步成功
- 修复：将 GitHub 版 `index.html` + `version.json` 复制进 `deploy_cs/` 并重新部署 CloudStudio；curl 验证 CloudStudio 返回 4.68MB、BUILD `20260911-0927`、数据到 9-10、`version.json` 同目录可 fetch ✅ 两网页现已一致。
- 工作区同步：`dashboard_offline.html` 覆盖为用户在线版，新增 `version.json`（根目录）；本地提交（领先 `github/main` 3）。`deploy_cs/` 同步。
- 待办：Gitee + GitHub 备份仓推送（本次 git push 沙箱旁路被用户拒绝，待确认或单位 `git pull` 后自行处理）。
- 经验/注意：
  1. 验证网页用 curl 抓源码 + grep/Python 提字段；WebFetch 对 SPA 不可靠（markdown 化丢 script）。
  2. CloudStudio 部署目录需同时放 `index.html` + `version.json`，`fetch('version.json')` 才成功。
  3. 用户新工作流 = 在线版（fetch version.json），以后更新需重新 BUILD 把数据硬编码进 JS，而非简单重嵌 base64。我工作区的 dashboard_offline.html 现已是此在线版。
  4. GitHub Pages 之前（9-05）被用户推过 cutoff=9-04 的旧版，本次 9-11 已被覆盖为 9-10 版。

## 17:0x 用户给新 Excel「2026年9月西北服务产品(2).xlsx」，AI 重建 2026-09 数据块并部署
- 用户从微信临时目录发来新 Excel（路径 `D:/xwechat_files/.../2026年9月西北服务产品(2).xlsx`），要求更新看板。
- **关键发现：`build_dashboard.py`（含 `build()` 主数据+大保养生成器）已丢失**——git 历史与磁盘均无，仅 `sync-kit/sync.py`（依赖它）和 `dashboard/_build.py`（旧单月自动同步脚本）还在。已从 git `c889fdb` 恢复 `sync-kit/` 与 `dashboard/_build.py`，并在工作区新增 **`gen_month.py`**（自包含重建工具）。
- `gen_month.py` 复用 sync.py 的 `build_tech`/`gen_extend`/`gen_weekly`（T/E/W），并**重建**了缺失的 主数据(D) 与 大保养(B) 解析器：
  - 主数据(D)：`服务产品收入统计` 按 (办事处,服务中心,服务网点) 聚合 15 品项+合计+来源；新增 `time` 字段（服务月进度，截止 9-10 → 日=14/周=2/总周=5，与旧版逐字一致）。
  - 大保养(B)：`清洗保养总单` 取 **销售分类(col16)=清洗保养**（含小/大保养/灶具保养/超范围子类，=348）；`剔除项`/`大保养项` 取 **服务收费项目(col13)**，且与清洗保养总单**可重叠**（独立 if 而非 elif），故 分母=348-8=340、大保养=7、占比 2.06%（与旧版完全一致）。
  - 周数据(W)：周起点改**向后**取服务月所在周周一（8/24，非 8/31），脚手架 5 周至 9-27，数据按截止 9-10 过滤（09/14、09/21 两周为空）。
- **校验**：新 Excel 汇总 `服务产品收入统计.合计` = ¥237,145.13，与主数据 sum 完全一致；D/T 与 9-11 BUILD 旧版**逐字节相同**，B/W/E 仅内部细节不同（聚合相同）。即本次 Excel 数据与 9-11 BUILD 一致，看板数值未变，但已用统一工具重建、结构一致。
- 部署：覆盖 `dashboard_offline.html`（BUILD 20260911-1702，version.json cut 2026-09-10）→ 复制 `deploy_cs/` 并**重新部署 CloudStudio**（同 URL 验证通过）✅ → Gitee `git push origin main`（`9675384..5796d06`）✅。
- **GitHub Pages 推送失败**：用记忆中 classic PAT `ghp_...Lp1` 调 GitHub API 返回 **401 Bad credentials**（已失效/被撤销）。故 GitHub Pages 仍停留在 9-11 BUILD（commit 41896a0e08，KPI 与本次一致，仅 B/W/E 内部细节不同）。需用户重发有效 PAT 或自行推送。
- 经验：① 多月份看板更新靠重建 `MDATA_2026_0X_*` 五块并注入；② `gen_month.py` 为以后改 Excel 更新的主工具（需改顶部 EXCEL 路径）；③ 推送 GitHub 现依赖有效 PAT。
