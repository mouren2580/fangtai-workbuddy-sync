# 2026-08-21 工作日志

## 08:0x 8-21 数据更新 + 截止日铁律锁 8-20
- 用户提交新 Excel `2026年8月西北服务产品.xlsx`（06:14 更新），业务日期已含 **8-21**（CSM项目/明细「服务实际完成时间」真实日期；WMS最大仍8-19；安装工单明细最大8-03）。文件更新日=8-21。
- 截止日口径：用户按「铁律」（更新日8-21前一天=8-20）选择截止 **2026-08-20**，剩余 **7 天（8.21–8.27）**（已过24天/共31天）。
- 改动（dashboard_offline.html）：
  1. 423 行基础 meta `统计截止日` 由 `2026-08-17` → `2026-08-20`（DEFAULT_ASOF 初始化即读此值）。
  2. `rebuildFromWorkbook()` 同步后不再用 `deriveCutoffFromWorkbook` 推算（会算出8-21覆盖），改为固定 `DEFAULT_ASOF=2026-08-20`、`统计截止日='2026-08-20'`。
  3. 离线加载路径同样固定 8-20，不再扫表推导。
- 重嵌新 Excel base64（3093104→3214140）。
- **修复前次错别字/未定义变量 bug**：离线加载注释「接日」→「截止日」；原 `cutDate` 变量在删掉推导后于 `document.title`/`console.log` 仍被引用导致 ReferenceError，已改为字面量 `'2026-08-20'`。
- CloudStudio 重新部署成功（地址不变）：https://0717bc4b30824b8d8a407555473b321e.app.workbuddy.link —— **已为最新（截止8-20、数据至8-21）**。

## 08:3x 推送情况（GitHub 网络抖动）
- 本地 git commit `879a708`（3 files, +18/-13）。
- **Gitee 主仓推送成功**：`96dca93..879a708 main -> main`。
- **GitHub Pages（fangtai-dashboard）推送失败**：`git clone` 三次均 `Failed to connect to github.com port 443` / `Connection was reset`。CloudStudio 已是新版，但 GitHub Pages 网页仍停在旧版（截止8-20锁之前）。待网络恢复补推，或用户在家网络推。
- **GitHub 备份仓（fangtai-workbuddy-sync）推送失败**：`RPC failed; curl 55 Send failure: Connection was reset` / `unexpected disconnect`。本地领先 `github/main` 1 提交，待网络恢复补推。
- 结论：看板数据已通过 CloudStudio 外网可见；GitHub 双仓待网络恢复后统一补推。

## 08:40 网络恢复，双仓补推成功（用户喊"继续"）
- **GitHub Pages（fangtai-dashboard）**：克隆成功→覆盖 index.html→push 第6次成功 `d3613e2..aeafad2 main -> main`（前5次 443 超时/重置）。远程 head=aeafad2。
- **GitHub 备份仓（fangtai-workbuddy-sync）**：第1次即成功 `685e3bf..879a708 main -> main`。本地与 github/main 完全同步。
- **三端已一致**：Gitee 主仓 + GitHub 备份仓 + 工作区本地，均含 8-21 更新（截止锁8-20）。CloudStudio + GitHub Pages 网页均为新版（截止8-20、数据至8-21）。
- 清理临时克隆目录 fhd_clone。
- 结论：本次 GitHub 推送共重试约 17 次 clone/push（06:33–08:40 间网络反复抖动），最终 08:40 全部落地。

## 待办
- PAT 替换（撤销 ghp_...Lp1 改受限 PAT）仍按用户"先不动"保持现状。
