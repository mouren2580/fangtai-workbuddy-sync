# 2026-09-15 方太西北服务产品看板 · 按参考版逻辑整体重建

## 背景
用户发来两份「单位版」参考文件 + 新 Excel，要求「按这个逻辑更新数据看板」：
- `dashboard.html`（用户单位版，BUILD `20260913-0918`，截止 2026-09-12）— 逻辑基准
- `服务产品达成看板_计算说明.html` — 口径说明（含 11 个模块的定义）
- `2026年9月西北服务产品(3).xlsx` — 新数据源（各表日期均为 2026-08-28 ~ 2026-09-14）

## 关键发现：参考版比工作区版「多一套模块」
参考 `dashboard.html` 与工作区 `dashboard_offline.html` **变量结构完全相同（各 124 个顶层声明）**，
但参考版多出/更新了 4 个块：
- `MONTHLY_FOUR` = `{months:{ "2026-01".."2026-09": {workorder, valueadded, valueparts, bigcare} }, year:{...}}`
- `EXTEND_TIME_DATA` = `{months:{"2026-09":[{d,o,t,c,yr,amt}], "2026-08":[...]}}`
- `CLEANING_DATA` = `{months:{"2026-09":[{o,n,e,ei,d,it,amt}]}, meta}`
- `MDATA_YYYY_MM_{D,T,B,W,E}` 的 9 月内容

## 最重要口径结论（已逐项验证）
**所有模块都按「统计截止日（D−1）」过滤**，不是「整服务月不过滤」。
验证方法：把新 Excel 按 9-12 重算，与参考版 BUILD `20260913-0918` 的全部数值对照 →
- workorder：总工单 11591 / 网点 97 / 工程师 237 / 止回阀 737 / 烟机安装 3860 / 占比 19.09% **全中**
- valueadded：全区总计数 1094、12 个单品逐一吻合
- valueparts：昨日 88 / 近3日 337 / 近7日 749 / 当月 1571 **全中**
- bigcare：397 / 388 / 11 / 2.84% **全中**；`net`/`eng` 明细（含合计行）也一致
- EXTEND_TIME_DATA：104 条、合计 ¥39,448.30 — **多重集完全一致**
- CLEANING_DATA：387 条 — **多重集完全一致**
- MDATA_B、MDATA_W 逐字节 IDENTICAL

## 新增工具（本次落地，以后刷新直接复用）
- `gen_v2_lib.py` — 自包含解析器：`build_workorder / build_valueadded / build_valueparts /
  build_bigcare4 / build_extend_time / build_cleaning`
- `build_month.py` — 入口：
  - `python build_month.py check 2026-09-12` → 与参考版逐块对照（校验用）
  - `python build_month.py build 2026-09-14` → 重建并注入 `dashboard_offline.html` + 写 `version.json`
  - 内置：以参考 `dashboard.html` 为底板、只替换 2026-09 分支（1-8 月与 `year` 分支保留）
- `gen_month.py` 保留，仅用于 D/T（服务产品收入统计）与 W（周数据）——**并修了一个 W 的口径 bug**：
  `CSM配件` 的 col0 是「大区」不是「办事处」，不能用来登记 网点→办事处 映射，
  否则 WMS 网点买断行会被错记到大区名下（当周 清洁耗材 归集从 5898/196 纠正为 5398/700 一类）。

## 关键坑（下次务必注意）
1. **排序**：大保养 `net`/`eng` 的同值排序用**首次出现顺序**（不是名称），合计行文案固定为
   `[合计]全区` / `[合计]全部网点` / `[合计]全部工程师`。
2. **meta 标签不统一**（照抄参考即可）：`workorder.meta.截止` = 服务月末（9-27）；
   `MONTHLY_FOUR.bigcare.meta.截止日` = 服务月末；`MDATA_*_B.meta.截止日` = 截止日（9-14）且**无「周期」字段**；
   `workorder.meta.周期` 固定 `YYYY-MM-DD~YYYY-MM-DD` 服务月末。
3. **延保**：`EXTEND_TIME_DATA` 记录里 `c` 恒为空串（参考版如此），E 块 `techs[].id` = 姓名（不是编码）；
   E 块必须由已过滤的 `rec_et` 聚合，不能用 gen_extend（它不过滤日期）。
4. `valueadded.products[]` **有** `verify` 字段（油杯垫=部分编码未匹配提示、止回阀=特殊口径）；
   `valueparts.products[]` **没有** `verify`。
5. 明细表（EXTEND_TIME / CLEANING）按源表行序，跨文件不可复现完全一致的排列——只需校验「多重集一致」。
6. `MDATA_2026_08_*` 不存在是正常的：8 月用基准块 `DASHBOARD_DATA/TECH_DATA/...`。

## 列映射（已实测）
- 工单表（表头第 1 行）：办事处 col11、服务完成时间 col41、工单编号 col53、服务工程师 col55、
  服务项目 col57、工单类型 col65、产品组 col73、服务网点 col79、工程师编号 col30
- CSM服务项目（表头第 2 行）：办事处 col1、服务网点 col3、服务工程师 col5、录入完成时间 col7、
  服务收费项目 col13、销售分类 col16、上缴金额 col24
- CSM配件（表头第 2 行）：办事处 col2、服务网点 col3、服务工程师 col5、编码 col6、配件名称 col12、
  配件编码 col13、销售分类 col17、数量 col18、上缴金额 col27、录入完成时间 col36
- 工单自购配件明细（表头第 1 行）：服务网点 col5、录入完成时间 col10、配件名称 col13、
  服务工程师 col17、配件数量 col19、物料编码 col23、物料名称 col27、工程师编号 col28
- WMS网点买断配件明细（表头第 2 行）：办事处名称 col1、CSM网点名称 col3、配件编码 col5、
  数量 col7、实收总金额 col8、销售分类 col10、发货时间 col11

## 本次产出（BUILD 20260915-0834，截止 2026-09-14，服务月 8.28–9.27，已过 18/31 天 = 58.1%）
- 主数据 D：89 网点，整体实际合计 **¥306,777.34**（8-12 时为 ¥275,839）
- 工单：总工单 **13,136**；防火阀占比 **19.02%**（止回阀 833 ÷ 烟机安装 4,379）
- 大保养：总单 459 / 分母 449 / 大保养 12 / 占比 **2.67%**
- 延保：113 单 **¥42,974.90**（近3日 ¥…，窗口 昨日 114 / 近3日 291 / 近7日 820 / 当月 1,774 为增值配件口径）
- 清洗保养：449 条（4 主项）… 明细 448 条；增值产品全区总计数 1,271
- 三模块「止回阀」互相校验一致 = **833** ✅
- JS 语法 `node --check` 通过；全部数据块 JSON 可解析

## 部署
- ✅ **Gitee 备份仓** 已推：`b4b1c15` → `d321e78`
- ⏸ **线上分享链接（CloudStudio）未更新** — 工具要求本轮明确确认；已向用户提问，待答复
- ✅ 离线副本 `dashboard.html`、同步版 `服务产品达成看板_计算说明.html` 已生成在工作区

## 附带修复：Gitee 推送 100MB 限制
本次 `git add -A` 误提交了 `2026-09-12-20-12-51/models/faster-whisper-small/model.bin`（**462MB**），
Gitee 报 `exceeds quota 100MB` 拒收。处理：
- 该目录只存在于最新一次提交（`35a0532`，未推送成功）→ `git rm -r --cached` + `commit --amend` 后历史中不再含大 blob
- `.gitignore` 新增：`2026-09-12-20-12-51/`、`models/`、`*.bin`、`_bak_*`、`_probe_*.py`、`_check_*.py`、`_diff_*.py`
- 清理了一次性校验脚本（`_probe_ref*.py` 等），保留 `gen_v2_lib.py` / `build_month.py` / `gen_month.py`

---

# 第二段（09:00–09:10）用户发来线上链接 → 三端对齐 + 两个真 bug 修复

用户只发了一个链接：`https://mouren2580.github.io/fangtai-dashboard/?newPanel=true&v=20260914-0913&_=1789433162707`

## 三端版本对比（抓下来实测）
| 端 | BUILD | 截止日 | 说明 |
|---|---|---|---|
| GitHub Pages | `20260914-0913` | 2026-09-13 | **用户自己推的**（9-14 09:13 构建） |
| CloudStudio | `20260911-1702` | 2026-09-10 | 落后，待重新部署 |
| 工作区 | `20260915-0834` → `20260915-0904` | 2026-09-14 | 铁律＝更新日−1 |

- 线上与本地 **JS 代码本体完全一致**（剥离数据块后仅 `var BUILD` 一行不同），`newPanel` 在页面里**没有任何对应代码**（无效查询参数，纯缓存穿透用）。
- 线上版 `version.json`：`{"v":"20260914-0913","cut":"2026-09-13","t":"2026-09-14T09:13:51"}`。

## 回归 bug①：换底板丢历史月（我上一版的问题）
线上版 `CLEANING_DATA.months` 有 **2026-08(788 条) + 2026-09**，我上一版只剩 2026-09。
原因：底板指向用户那份 **9-12 参考版**，它自身就没有 8 月清洗明细，而 build 只替换当月分支 → 历史月被"继承丢失"。
- 8 月 Excel（`2026年8月西北服务产品.xlsx`，8-21 快照）只能复现 610 条（数据止于 8-20），
  线上那 788 条含 8-21~8-27 的 110 条 → **无法从本地源表重建，只能以线上版为准**。
- 修复：① 把线上版存为工作区**新底板 `dashboard_ref.html`**，`build_month.py` 的 `REF` 改指向它；
  ② `build_month.py` 新增 `merge_retained()`：换底板时自动把旧版有、新版没有的**月份/块补回来**
  （`MONTHLY_FOUR`/`EXTEND_TIME_DATA`/`CLEANING_DATA` 的 months 缺月、以及顶层 `MDATA_*` 缺块）。

## 真 bug②：自购配件表的"办事处"取错列
`工单自购配件明细` **根本没有办事处列**（col15 = 「大区部」、col12 = 「服务中心」、col5 = 「服务网点」）。
原代码 `ZG_OFFICE=15` 直接当办事处用 → 凡只在自购配件表出现的耗材
（洗碗粉/洗碗盐/漂洗剂/厨小护/蒸烤清洁剂/锅支架清洁膏）的技师归属全被标成 **「西北大区部」**（数量对、归属错）。
- 修复：新增 `tech_office` / `net_office` 反查表（从 `CSM配件` col2=办事处 + col5=工程师 + col6=工程师编码 建），
  自购行取 `tech_office[编码] → tech_office[姓名] → net_office[网点]`；原 `ZG_OFFICE` 改名 `ZG_ALGO` 防误用。
- 互校：`CSM配件` 建出 212 个 技师编码→办事处 映射、`工单表` 建出 238 个，**两者 0 个一对多**，
  且与参考版归属完全吻合（薛雷→宁夏、段亚南→包头…）。修完 `valueparts` 12 个产品（含每位技师的 office）**100% 一致**。

## 口径补齐（照参考版实测）
1. `剔除项`/`大保养项` 两处**顺序不同**：`MDATA_*_B.meta` 用**常量序**（["保养超范围收费","灶具保养"] / ["油烟机大保养升级包","油烟机大保养"]）；
   `MONTHLY_FOUR.bigcare.meta` 用**源表首现序**（["灶具保养","保养超范围收费"] / ["油烟机大保养","油烟机大保养升级包"]）。
   已实现为 `build_bigcare4` 额外返回 `enc` 两个清单，`build_month.py` 只给 MF 分支套用（`bc_mf.pop("enc")` 防止多字段）。
2. `valueparts` 的配件名清单 `pname` 也按**源表首现序**（油杯垫 → ["…K110","…K40"]）。
3. E 块逐年金额 **不做四舍五入**（参考版保留浮点尾差如 `42616.90000000001`），只有 `tamt` 和 `total` 舍入/原值按参考照抄。
4. 明细表同日内顺序＝源表行序、工程师同值排序＝源表行序：**跨 Excel 快照不可复现**，只校验多重集。

## 校验结果（截止 9-13 反算 vs 用户 9-14 版）
`workorder` / `valueadded` / `valueparts`(含归属) / `MONTHLY_FOUR.bigcare` / `CLEANING_DATA` / `MDATA_*_W` **全部一致**；
`EXTEND_TIME_DATA` 多重集一致（仅同日行序不同）；`E` 仅同值排序不同；
`D`/`T` 差异属**预期**——它们取「报表全量」不过滤日期，两份 Excel 快照不同（ref 报表总合计 ¥289,121.9 vs 本次 ¥306,777.34）。

## 本次产出（BUILD `20260915-0904`，截止 2026-09-14，服务月 8.28–9.27，已过 18/31 = 58.1%）
D **¥306,777.34**（89 网点）｜工单 **13,136**｜工程师 212 人 / 工单消耗 ¥261,641.40
止回阀 **833** / 烟机安装 4,379 / 防火阀占比 **19.02%**｜大保养 459 / 449 / **12** / **2.67%**
延保 **113 单 ¥42,974.90**（E 45 人，y3 ¥42,616.90）｜清洗明细 448｜增值产品 1,271｜增值配件当月 1,774
三模块止回阀互校 **833 = 833** ✅｜JS `node --check` 通过｜文件 4.62 MiB

**历史数据 100% 保留校验**：CLEANING 08(788) 与线上逐条一致、EXTEND 08(139) 一致、
MONTHLY_FOUR 01–08 全 8 个月一致 + `year` 一致、MDATA 01–07 共 35 块一致、8 个基准块一致。

## 部署
- ✅ Gitee 已推：`fd5228c..fe0686d`
- ⛔ GitHub Pages **推不动**：PAT 仍 `401 Bad credentials`；线上现有版本是用户自己推的 `20260914-0913`
- ⏸ CloudStudio 仍停在 `20260911-1702`，新版本已放进 `deploy_cs/`，**等用户当轮确认后部署**
- ✅ 离线副本 `dashboard.html` 已同步

---

# 第三段（09:07–）用户反馈「咱们这个看板没更新」→ CloudStudio 部署成功

用户只说了一句「咱们这个看板没更新」。抓三端确认：工作区/`deploy_cs` 是 `20260915-0904`（最新）、
GitHub Pages `20260914-0913`、CloudStudio `20260911-1702` —— **内容是好的，只是没推上去**。

## ✅ CloudStudio 已更新成功（关键：工具报错≠部署失败）
用户确认后调用 `workbuddy_sites_deploy`（`updateExistingApp:true` 会报「no existing app」，去掉后即可），
工具**最终返回错误**：
`应用预留域名 fangtai-dashboard.app.workbuddy.host 未绑定到本次发布环境，本次发布已停止。`

但查 `~/.workbuddy/logs/sites/sites-deploy-*.log` 发现 **内容其实已经上传并 release 成功**：
- 上传 790164 字节 → sandbox `0717bc4b30824b8d8a407555473b321e` → 服务验证 200
- `artifactRelease` 返回 **201**，`releaseUrl = https://0717bc4b30824b8d8a407555473b321e.app.workbuddy.host/`
- 报错仅发生在最后的「预留域名绑定校验」（平台域名体系 `.link` → `.host` 迁移所致，
  `applications/resolve` 现场新建了 appId `wbapp_h3EIjDV5DM6sas6wF774Px`）

**实测三个域名全部返回新版** `{"v":"20260915-0904","cut":"2026-09-14"}`：
- `https://0717bc4b30824b8d8a407555473b321e.app.workbuddy.link`（用户常用旧链接）✅
- `https://0717bc4b30824b8d8a407555473b321e.app.workbuddy.host` ✅
- `https://2089907644512342016.app.workbuddy.host` ✅
- `https://fangtai-dashboard.app.workbuddy.host` ❌ 返回的是默认页（未绑定，别用）

**⚠️ 经验：遇到这个域名报错不要以为失败。** 用
`curl -s <链接>/version.json` 验证 `v`/`cut` 即可确认是否真的生效。

线上页面本体校验：BUILD `20260915-0904` ｜ CLEANING `{09:448, 08:788}`（历史月保留✅）
｜ EXTEND `{09:113, 08:139}` ｜ 9 月 89 网点 ¥306,777.34 ｜ 周数 5 ｜ 大保养 459/449/12/2.67%（截止 9-14）。

## ⛔ GitHub Pages 仍推不动
本机无 `gh` CLI；`git remote` 里 GitHub 只有备份仓 `fangtai-workbuddy-sync`，
Pages 仓 `fangtai-dashboard` 无本地 remote；PAT `ghp_...Lp1` 再测仍 **HTTP 401**。
→ 只能用户①网页自行推，或②提供新 PAT（fine-grained，仅 `fangtai-dashboard`，Contents RW）。
