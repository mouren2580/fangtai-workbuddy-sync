# 2026-08-13 工作日志

## 两台电脑（家里/单位）任务同步搭建完成
- **方式**：Gitee 私有仓库（GitHub 在国内访问超时，故改用 Gitee）。
- **仓库地址**：https://gitee.com/xbftsjkb/fangtai-workbuddy-sync.git （分支 `main`，私有）。
- **本机（家里）SSH 密钥**：`~/.ssh/id_ed25519_gitee`，公钥已在 Gitee 以标题 `home-workbuddy` 添加；`~/.ssh/config` 已映射 `gitee.com` 使用该密钥。
- **仓库根目录**：`D:\WorkBuddy`，已 `git init -b main` 并首次推送成功。
- **.gitignore 约定**：排除 `.codebuddy/`（机器本地设置）；保留 `.workbuddy/`（工作区记忆同步）、`*.xlsx`、`Claw/`、`README.md`、`.gitignore`。
- 已处理：首次推送时因未知主机密钥报 `Host key verification failed`，用 `ssh-keyscan` 写入 `~/.ssh/known_hosts` 后解决。

## 下一步（单位电脑）
1. 在单位电脑 clone 仓库：`git clone git@gitee.com:xbftsjkb/fangtai-workbuddy-sync.git`
2. 生成该机 SSH 密钥并加入同一 Gitee 账号的 SSH 公钥列表（可多密钥共存）。
3. 即可双向 `git pull` / `git push`。

## 日常同步习惯（两台都可改）
- 改动后：`git add -A && git commit -m "说明" && git push`
- 另一台开机先：`git pull`
- 若两边同时改了同一文件，先 `git pull` 解决合并冲突再 `git push`。
