# 2026-08-20 工作日志

## 推送网页看板更新（最新 Excel 内嵌 + 双仓库同步）
- 用户诉求：用最新 `2026年8月西北服务产品.xlsx`（mtime 08:33，数据较 8/19 版有变动）推送网页看板更新。
- **离线看板重生成**：`dashboard_offline.html` 内 `<script id="xlsxData" type="text/plain">` 的 base64 用当前 xlsx 整体替换（Python 正则替换，保留"日期自动从过账日期推断"逻辑）。校验：内嵌 sha256 == 磁盘 sha256（True）。
- 同步 `deploy_cs/index.html`（`cp dashboard_offline.html deploy_cs/index.html`），重部署 CloudStudio（同一地址 `https://0717bc4b30824b8d8a407555473b321e.app.workbuddy.link`）。线上抓取校验内嵌 xlsx 与本地一致（True）。
- **提交**：`54a4233`「更新网页看板：内嵌最新 2026-08-20 版 Excel 数据并重部署 CloudStudio」（含两个 xlsx 文件改动 + dashboard_offline.html + deploy_cs）。
- **推送 Gitee**：SSH + 沙箱旁路，`0f31276..54a4233 main->main` 成功。
- **推送 GitHub**：首次失败（沙箱拦 443），加 `dangerouslyDisableSandbox:true` 后 `8740542..54a4233 main->main` 成功。token 仍仅本次内存、未写 .git/config（已核验 `git remote -v` 含 0 行 token）。
- 当前双仓库（Gitee 私有 / GitHub Public）均对齐到 `54a4233`，本地不再领先。

## 待办（延续自 08-19）
- GitHub 仓库仍 Public → 建议改 Private（用户 08-19 说"1暂时不动"）。
- PAT `ghp_EPF3...Lp1` 曾在聊天中粘贴 → 建议撤销/重发（用户说"2处理"，疑自行处理；本次推送仍使用该 token，推送后再次提醒）。
- 家里本地看板备选：`python -m http.server 8090` + 打开 `dashboard/local.html`（未启用）。

## 更新原 GitHub 看板 fangtai-dashboard（2026-08-20 续）
- 用户诉求：把最新数据更新到 `https://mouren2580.github.io/fangtai-dashboard/`。
- **探查**：仓库 `main` 分支（public），Pages 源 `main`/根目录。`index.html`（2.76MB）**无内嵌 xlsx**——数据来自 JS 硬编码的旧 `DASHBOARD_DATA`（显示 8/17 快照），仅有「同步Excel」按钮手动上传。
- **方案**：浅克隆（`--depth 1` 才成功，深克隆在沙箱旁路下静默失败并清目录）→ 用 `dashboard_offline.html`（已含 8/20 xlsx + 保留同步按钮）替换 `index.html`，原版备份为 `index.orig.html` → 设本地 git 身份（浅克隆缺 user）→ commit → push（`dangerouslyDisableSandbox` + PAT `insteadOf`）。
- **推送成功**：`542f6a2..d5e20ca main->main`。git blob sha256 与 offline 版一致（`5ba429c1...`）。GitHub Pages 自动重建（约 1-3 分钟）。
- **关键坑**：① `git clone` 必须 `--depth 1`，否则沙箱旁路下静默失败清空目录；② 浅克隆缺 user 身份，commit 前需 `git config user.email/user.name`；③ GitHub API `contents` 对 >1MB 文件 `content` 为空（仅 `size` 有效），改用 `git show HEAD:index.html | sha256` 校验；④ 沙箱 DNS 拦 `raw.githubusercontent.com`（python urllib 报 getaddrinfo failed），但 `github.com`/`api.github.com`/`mouren2580.github.io` 可达；⑤ `curl` 写 body 被沙箱禁（报 write error），用 `-o /dev/null -w` 或 python 管道。
- **现状**：原 GitHub 看板现已内嵌最新 8/20 数据，打开即显示（不再依赖手动点同步）；同步按钮仍保留。日后更新：改 Excel → 喊我 → 重嵌 offline + 克隆替换 + 推 → 等 Pages 重建刷新。
