# 2026-08-20 工作日志

## 上午：看板日期口径确认与双备份补推
- 用户确认口径：截止日期 8-19、数据更新 8-20、数据覆盖 8-19。
- openpyxl 扫描 Excel 所有「日期/时间」列：WMS收货明细/CSM项目/CSM明细最大日期均 2026-08-19，安装工单明细 8-03；整体 OVERALL_MAX=2026-08-19。验证看板截止日应锁定 8-19，不被文件更新日 8-20 带偏。
- 关键发现：`dashboard_offline.html` 内嵌 xlsx 的 base64 已与今天 8-20 08:33 的 `2026年8月西北服务产品.xlsx` 逐字节相等（长度 3093104），即数据早已是最新，无需重嵌；`deriveCutoffFromWorkbook()` 扫表即得出 8-19。
- CloudStudio 重新部署成功，地址不变：https://0717bc4b30824b8d8a407555473b321e.app.workbuddy.link
- GitHub 网络恢复；`fangtai-workbuddy-sync` 备份仓补推成功（commit 7ffd717，59ab9f1..7ffd717，含新增 check_xlsx_dates.py / reembed_xlsx.py），本地与 github/main 已同步，双备份闭环完成。
- GitHub Pages（fangtai-dashboard）：上次推送的 index.html 内嵌 base64 与今日 Excel 一致，已是最新（截止 8-19），无需重推。

## 上午稍后：PAT 安全替换决策
- 用户决定：撤销当前 classic PAT（`ghp_...Lp1`，scope=`repo`=全仓库读写，过度授权，过期 2026-09-18），替换为 fine-grained 受限 PAT（仅 `fangtai-dashboard`+`fangtai-workbuddy-sync` 两仓库，Contents:RW）。
- 限制确认（AI 能力边界）：① 创建 fine-grained PAT 须用户在 GitHub 网页生成，AI 不能代建；② 撤销 classic PAT 无法经 API 自删（需 basic auth），须用户网页手动撤销。
- 切换顺序（避免中断）：用户先建新 fine-grained PAT 并发 AI → AI 切换 insteadOf 并验证 push → 用户再网页撤销旧 classic。当前旧 PAT 仍保留使用中。
- 详见项目长期记忆 MEMORY.md「GitHub 双备份远程 / PAT 安全策略」。

## 上午再稍后：修复截止日被单号字符串误判为 8-20
- 用户截图反馈看板显示「截止 2026-08-20」「数据覆盖至 2026-08-20」，但实际业务数据只到 8-19。
- 排查：Python openpyxl 扫描 Excel 真实业务日期最大为 8-19；但 JS 的 `deriveCutoffFromWorkbook()` 对字符串做宽松 `new Date()` 解析，把业务单号（如 `CK20260802000012`、`FGDG20260802005133`）里的 `20260820`/`20260802` 子串误解析为 2026-08-20，导致 maxDate=8-20。
- 修复：修改 `deriveCutoffFromWorkbook()`，只接受 SheetJS 已解析为 `Date` 对象的单元格，忽略字符串/数字的宽松解析。
- CloudStudio 重新部署成功，地址不变：https://0717bc4b30824b8d8a407555473b321e.app.workbuddy.link
- GitHub Pages（fangtai-dashboard）推送因 GitHub 443 网络抖动失败，待恢复后补推；本地工作区已 commit（ce42ef0），`fangtai-workbuddy-sync` 备份仓再次落后 1 提交。
- 新增调试脚本：`find_0820.py`（按日期列扫最大值）、`find_0820_strings.py`（找所有可能被 JS 解析为 8-20 的字符串/日期）。
