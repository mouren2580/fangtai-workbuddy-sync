# 2026-09-18 工作日志

## 看板数据更新（Excel(3) → 截止 2026-09-17）

用户发来新导出的 `2026年9月西北服务产品(3).xlsx`（9-18 09:12 落盘，数据到 9-17），按铁律锁 **截止日 2026-09-17**。

### 🐞 关键事故：平台导出改了列顺序（差点静默出错数据）
- 首轮 `check` 发现异常：算出的办事处名为空、办事处数只剩 2、技师编码读成「已支付」、工程师数 445。
- 定位：**「工单」表列序变了** —— `办事处名称` 由 L(11) → K(10)，`工程师编号` 由 AE(30) → AF(31)。其余列（服务网点 CB、服务工程师 BD、服务完成时间 AP、服务项目 BF、工单类型 BN、产品组 BV、工单编号 BB）位置未变。
- 其余表全部核对无误：CSM服务项目（表头行 2）、CSM配件（表头行 2）、工单自购配件明细（表头行 1）、服务产品收入统计（表头行 2）、服务技师、WMS网点买断配件明细（表头行 2）。
- **危险点**：列错位不会报错，只会「数量对、归属错」。

### 修复（已写进解析库，防复发）
1. `gen_v2_lib.py` 新增 `_cols(ws, header_row, spec, tag)`：**按表头名解析列号**，找不到才回退常量并打印 `[warn]`。
2. `WO_HEADERS`（工单表 9 列）、`ZG_HEADERS`（自购配件明细 8 列）→ `build_workorder` / `build_valueparts` 改用解析结果 `C[...]` / `Z[...]`。
3. `verify_columns(excel)` + `HEADER_SPEC`：核对 CSM配件/CSM服务项目/服务产品收入统计/WMS买断 的固定列号与表头名是否仍一致；`build_month.make_blocks()` 开头自动调用（本表头行 2）。
4. 兜底常量同步更新为 `WO_OFFICE=10`、`WO_TCODE=31`。

### 验证方法（值得复用）
- 先把上一版 `dashboard_offline.html` 存成 `_prev915.html`，`build` 前后对比。
- 写了 `_check_diff915.py`：**语义比对**脚本 —— 字典列表**递归多重集**比较，能区分「仅顺序不同（明细行序不可复现）」与「真实差异」。
- 结果（新 Excel 按旧截止日 9-15 重算 vs 上一版构建）：`workorder`/`valueadded`/`W` **完全一致**；`valueparts`/`bigcare`/`B`/`E`/`EXTEND_TIME_DATA`/`CLEANING_DATA` **仅顺序不同**；只有 `D`/`T` 有真实差异 —— 二者本就是「报表全量」口径，换快照必变，**属正常**。→ 列序修复正确、历史数据无修订。

### 构建结果（BUILD 20260918-1235，截止 2026-09-17）
| 指标 | 9-15 版 | 本版 |
|---|---|---|
| 工单总数 | 13,886 | **15,323** |
| 止回阀 / 烟机安装 / 占比 | 884 / 4,663 / 18.96% | **986 / 5,171 / 19.07%** |
| 大保养 | 13÷465＝2.80% | **14÷511＝2.74%**（总单 521）|
| 延保明细 | 115 条 ¥43,661.90 | **134 条 ¥50,692.20** |
| 清洗保养明细 | 464 条 | **510 条** |
| 增值产品 / 配件当月 | 1,349 / 1,868 | **1,495 / 2,095** |
| D 报表全量 | ¥321,560.84（91 网点）| **¥356,584.74（92 网点）** |
| T 工程师 | 214 人 ¥274,581.90 | **216 人 ¥306,811.80** |

- 增量自洽：工单 15,323 = Excel 中服务完成时间有效的行数；9-14→9-17 四天分别 777/750/682/755。
- 历史月完整：`MONTHLY_FOUR` 1–8 月 + `.year`、8 月清洗 788 条、8 月延保 139 条全部一致；40 个 `MDATA` 块一个不少；百分比两位小数与环形图字号 10.5 均未丢。

### 发布
- 四副本同步（`dashboard_offline.html`/`dashboard.html`/`deploy_cs/index.html` md5 一致）+ `version.json` = `{"v":"20260918-1235","cut":"2026-09-17"}`。
- GitHub Pages 已推：commit `eb6681d`（`push_gh_pages.py`，前台 + 沙箱旁路）。
- CloudStudio：待用户确认后部署。
- `.gitignore` 追加 `_prev*.html`（临时对照副本）。

### 环境备忘
- 解析 Excel 用 venv python（`envs/default/Scripts/python.exe`）；裸 bash 里 `dirname`/`git` 都不在 PATH，命令要带 PortableGit 前缀。
- `python /d/WorkBuddy/x.py` 会被解析成 `D:\d\WorkBuddy\x.py`（报 no such file）→ **用 `D:/WorkBuddy/x.py` 或先 `cd`**。
